"use client";

/**
 * @deprecated Use `ProductProcessFlow` — step is derived from the route via `journey.ts`.
 * Kept for docs and gradual migration; not used in FilingLayout.
 */
export { ProductProcessFlow as MacroStepper } from "./ProductProcessFlow";
